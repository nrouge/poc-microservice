spring.application.name: ms-configuration
spring.cloud.config.server:
  bootstrap: true
  prefix: /configuration
  git:
    uri: file://${user.home}/config-repo
    search-paths: apps,files,env/${MS_ENV:local}
spring.profiles.active: env-${MS_ENV:local}